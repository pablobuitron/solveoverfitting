# Model architecture configuration for Temporal Fusion Transformer
# Versión compacta para reducir overfitting en split grouped

hidden_size: 64               # antes 256
lstm_layers: 1                # antes 2
attention_head_size: 4        # antes 16
hidden_continuous_size: 32    # antes 128
output_size: 7                # 7 cuantiles para QuantileLoss
